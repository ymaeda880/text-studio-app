# -*- coding: utf-8 -*-
# text_studio_app/lib/wordtex/explanations/overview.py
# ============================================================
# wordTex 詳細説明：概要
# ============================================================

from __future__ import annotations


OVERVIEW_TEXT = """

##### 1. このページの目的

このページは，TeXのようにテキスト形式で文書の元を作成し，
そのソーステキストをプログラムで処理してWord報告書を作成するためのページです．

ソーステキストの拡張子は `.wtex` とします．

---

##### 2. wordTexの考え方

通常のWord編集では，文章，見出し，図，表，目次，脚注などを
Word画面上で直接編集します．

wordTexでは，まずテキスト形式で文書の元を作成します．
その後，プログラムがソーステキストを解析し，
Word文書として整形します．

この方式にすると，次のような処理を自動化しやすくなります．

- 報告書レイアウトの統一
- section番号の自動付与
- 図番号の自動付与
- 本文中の参照置換
- 図表の配置
- Word文書の再生成
- Gitなどによる差分管理

---

##### 3. 現在の実装範囲

現在は，次の要素を中心に実装します．

- 通常文章
- `%` コメント
- `\\set{...}` による設定
- `\\section{...}`
- `\\subsection{...}`
- `\\subsubsection{...}`
- section番号の表示形式設定
- `figureTable` による画像表
- 図番号の自動付与
- `\\ref{...}` による参照
- Word作成
- Wordプレビュー
- Wordダウンロード

---

##### 4. 基本的な処理の流れ

wordTexの処理は，次の順番で行います．

- `.wtex` ソースを読み込む
- ソーステキストを上から順に解析する
- コメント行を読み飛ばす
- 通常文章，section，set設定，figureTableブロックに分ける
- 1パス目でlabelを事前登録する
- 2パス目でWord文書を作成する
- 本文中の `\\ref{...}` を登録済み番号へ置換する
- Word文書をPDF化してプレビューする
- Word文書を保存する

---

##### 5. wordTexの基本方針

wordTexでは，できるだけ次の形に統一します．

    コマンド{本文,属性="値"}

たとえば section は次のように書きます．

    \\section{はじめに,label="sec:intro"}

figureTable も属性で指定します．

    \\begin{figureTable,
    caption="現場写真",
    label="fig:site",
    row=2,
    bc="lightgreen"}

本文中の参照は，TeXと同じように `\\ref{...}` を使います．

    図\\ref{fig:site}に示すように，
    第\\ref{sec:intro}章で説明します．

</div>
"""